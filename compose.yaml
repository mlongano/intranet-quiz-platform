services:

  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  db:
    security_opt:
      - apparmor:unconfined
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: quizparty
      POSTGRES_USER: quizparty
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quizparty -d quizparty"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

  # ── QuizParty application ───────────────────────────────────────────────────
  app:
    security_opt:
      - apparmor:unconfined
    build:
      context: .
      args:
        VITE_GOOGLE_OAUTH_CLIENT_ID: ${VITE_GOOGLE_OAUTH_CLIENT_ID:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env          # loads all vars from .env (see .env.example)
    environment:
      # Override / inject Docker-specific values on top of .env
      DATABASE_URL: postgresql://quizparty:${POSTGRES_PASSWORD}@db:5432/quizparty
      IMAGES_BASE: /app/images
    ports:
      - "${APP_PORT:-5002}:5001"
    volumes:
      - app_images:/app/images        # quiz images (persistent across deployments)
      - app_backups:/app/backups      # pg_dump + tar archives

  # ── Background LLM grading worker ──────────────────────────────────────────
  worker:
    security_opt:
      - apparmor:unconfined
    build:
      context: .
      args:
        VITE_GOOGLE_OAUTH_CLIENT_ID: ${VITE_GOOGLE_OAUTH_CLIENT_ID:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql://quizparty:${POSTGRES_PASSWORD}@db:5432/quizparty
      IMAGES_BASE: /app/images
    volumes:
      - app_images:/app/images
      - app_backups:/app/backups
    command: ["python", "-m", "services.llm_jobs", "worker"]

  # ── Automatic backups ──────────────────────────────────────────────────────
  backup:
    security_opt:
      - apparmor:unconfined
    image: postgres:16-alpine
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGHOST: db
      PGPORT: 5432
      PGDATABASE: quizparty
      PGUSER: quizparty
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      BACKUP_DIR: /backups
      IMAGES_DIR: /images
      BACKUP_INTERVAL_SECONDS: ${BACKUP_INTERVAL_SECONDS:-21600}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
      BACKUP_ON_START: ${BACKUP_ON_START:-1}
    volumes:
      - app_backups:/backups
      - app_images:/images:ro
      - ./scripts/backup_loop.sh:/usr/local/bin/quizparty-backup:ro
    command: ["sh", "/usr/local/bin/quizparty-backup", "loop"]

volumes:
  postgres_data:   # PostgreSQL data directory
  app_images:      # uploaded quiz images
  app_backups:     # database and image backup archives
